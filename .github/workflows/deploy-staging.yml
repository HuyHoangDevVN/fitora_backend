name: Legacy Windows deployment disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "The Windows/IIS staging deployment is retired. Use Deploy backend to Ubuntu."
          exit 1
